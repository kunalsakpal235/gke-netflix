apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: db-secrets
  namespace: streaming
spec:
  provider: gke
  parameters:
    secrets: |
      - resourceName: "projects/devops-1-502311/secrets/db-password/versions/latest"
        path: "db-password"
