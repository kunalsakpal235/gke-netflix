apiVersion: v1
kind: Service
metadata:
  name: playback-service
  namespace: streaming
spec:
  type: ClusterIP
  selector:
    app: playback-service
  ports:
    - name: http
      port: 80
      targetPort: 8093
